/*
 * Site-wide style constants.
 */

const color = {
  lightBackground: '#ededed',
  primaryFontLightBackground: '#222'
};

const size = {
  titleFontPx: 30,
  normalFontPx: 18
};

export { color, size };
